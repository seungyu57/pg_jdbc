{
  "meta": {
    "label": "PostgreSQL(JDBC)",
    "description": "Read PostgreSQL tables through JDBC (uses credentials preset)",
    "icon": "icon-database"
  },
  "readable": true,
  "writable": false,
  "params": [
    { "name": "sep_conn", "label": "Connection", "type": "SEPARATOR" },

    { "name": "host", "label": "Host", "type": "STRING", "mandatory": true },
    { "name": "port", "label": "Port", "type": "INT", "defaultValue": 5432, "mandatory": true },

    {
      "name": "pg_credentials",
      "label": "Credentials preset",
      "type": "PRESET",
      "parameterSetId": "pg-credentials",
      "mandatory": true,
      "description": "Select a DSS preset containing user/password"
    },

    { "name": "sep_table", "label": "Table", "type": "SEPARATOR" },
    { "name": "schema", "label": "Schema", "type": "STRING", "defaultValue": "public", "mandatory": true },
    { "name": "table", "label": "Table", "type": "STRING", "mandatory": true },

    { "name": "sep_adv", "label": "Advanced", "type": "SEPARATOR" },

    {
      "name": "fixed_db",
      "label": "Fixed database name",
      "type": "STRING",
      "defaultValue": "dataiku",
      "mandatory": true,
      "description": "DB selection removed: connector always connects to this database."
    },

    {
      "name": "jar_path",
      "label": "JDBC driver jar path",
      "type": "STRING",
      "mandatory": false,
      "visibilityCondition": "0",
      "description": "Hidden. Driver version selection removed; use fixed jar path on server."
    }
  ]
}