{
  "meta": {
    "label": "PostgreSQL(JDBC)",
    "description": "Read PostgreSQL tables through JDBC (fixed host/port)",
    "icon": "icon-database"
  },
  "readable": true,
  "writable": false,
  "params": [
    {
      "name": "pg_credentials",
      "label": "Credentials preset",
      "type": "PRESET",
      "parameterSetId": "pg-credentials",
      "mandatory": true
    },
    {
      "name": "schema",
      "label": "Schema",
      "type": "SELECT",
      "mandatory": true,
      "getChoicesFromPython": true,
      "triggerParameters": ["host", "port", "database", "pg_credentials"]
    },
    {
      "name": "table",
      "label": "Table",
      "type": "SELECT",
      "mandatory": true,
      "getChoicesFromPython": true,
      "triggerParameters": ["host", "port", "database", "pg_credentials", "schema"]
    }
  ]
}